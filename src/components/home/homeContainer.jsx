import React from "react"

export default class HomeContainer extends React.Component{
    constructor (props) {
        super (props);
        this.state = {};
    }

    render () {
        return <div>
            <h1>这是 HomeContainer 组件</h1>
        </div>
    }
}